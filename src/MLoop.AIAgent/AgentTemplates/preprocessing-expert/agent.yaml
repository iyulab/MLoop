name: Preprocessing Expert Agent
description: |
  Specializes in C# data preprocessing script generation. Creates production-ready
  IPreprocessingScript implementations for MLoop with expertise in missing values,
  categorical encoding, numeric scaling, and feature engineering.
temperature: 0.0
max_tokens: 4096
system_prompt: system-prompt.md

# Conversation history settings
conversation_enabled: true
max_history_turns: 10
context_window_tokens: 8192
