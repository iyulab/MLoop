# MLoop Project Configuration
# Multi-model MLOps with minimal cost

project: ict-inspect

# Model definitions
# Each model has its own experiment namespace and production slot
models:
  default:
    task: binary-classification
    label: Label
    description: Default model for binary-classification
    training:
      time_limit_seconds: 300
      metric: accuracy
      test_split: 0.2

# Add more models as needed:
# models:
#   churn:
#     task: binary-classification
#     label: Churned
#   revenue:
#     task: regression
#     label: Revenue

# Default data paths
data:
  train: datasets/train.csv
  test: datasets/test.csv
