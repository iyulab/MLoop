# MLoop AI Agent Example Project Configuration
project_name: mloop-agents-example
task: binary-classification
label_column: Purchased

# Training settings (optional - defaults provided)
training:
  time_limit_seconds: 300
  metric: F1Score
  test_split: 0.2

# Data paths (optional)
data:
  train: datasets/sample-data.csv
  test: datasets/sample-data.csv

# Model output (optional)
model:
  output_dir: models/staging
